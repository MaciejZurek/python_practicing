from calculator import creator, add, subtract # we can do this to make some names available in this namespace, but it increases the chance of names collision
from math import sqrt

print(creator)
print(sqrt(3))

